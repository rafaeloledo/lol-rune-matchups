{
  "name": "lol-rune-matchups-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript build for the LoL Rune Matchups frontend",
  "scripts": {
    "build": "tsc -p frontend",
    "watch": "tsc -p frontend --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
